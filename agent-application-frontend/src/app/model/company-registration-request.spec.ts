import { CompanyRegistrationRequest } from './company-registration-request';

describe('CompanyRegistrationRequest', () => {
  it('should create an instance', () => {
    expect(new CompanyRegistrationRequest()).toBeTruthy();
  });
});
